# Environment files — NEVER commit real secrets
.env
.env.local
.env.*.local
backend/.env
frontend/.env
*.env

# Dependencies
node_modules/

# Build output
dist/
build/

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Piper OSS training/inference source tree — NOT the deployable Piper service.
# This directory is the upstream piper project cloned for reference only.
# The deployable service lives at services/piper/.
piper-master/
